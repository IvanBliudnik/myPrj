import styled from "styled-components";
import {Theme} from "../styles/Theme";


export const SectionTitle = styled.h2`
  color: ${Theme.colors.font};
  text-align: center;
  position: relative;
  margin: 240px 100px 24px;
`